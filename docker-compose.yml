version: "3.9"

services:
  web:
    build: .
    container_name: secret-prediction-vault
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - bun_modules:/app/node_modules
    environment:
      - NODE_ENV=development

volumes:
  bun_modules:
